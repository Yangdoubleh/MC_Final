<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.MemberMenuDAO">	
	
	<select id="membermenuList"  resultType="membermenuVO">
		select * from membermenu order by menuNo desc
	</select>
	
	<insert id="membermenuWrite" parameterType="membermenuVO">
	<![CDATA[
		insert into membermenu( menuNo, memberID, foodName, foodDate) 
		values(#{menuNo}, #{memberID}, #{foodName}, #{foodDate} )
	]]>
	</insert>

	
</mapper> 
